const { User } = require('./user');
const { Config } = require('./config');
const { History } = require('./history');
const { Process } = require('./process');
const { Domain } = require('./domain');
const { WhoisServer } = require('./whoisServer');

module.exports = {
  User,
  Config,
  History,
  Process,
  Domain,
  WhoisServer,
};
